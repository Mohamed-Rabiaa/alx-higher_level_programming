#!/usr/bin/python3
import hidden_4
if __name__ == "__main__":
    list = dir(hidden_4)
    len = len(list)
    for i in range(0, len):
        if !(list[0].startwith("__")):
            print(list[i])
